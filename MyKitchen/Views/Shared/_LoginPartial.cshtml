@if(Request.IsAuthenticated) {
    @Html.ActionLink("Log out", "Logout", "Users", routeValues: null, htmlAttributes: new { data_icon = "gear" })
}
else {
    @Html.ActionLink("Log in", "Index", "Users", routeValues: null, htmlAttributes: new { data_icon = "gear" })
}
